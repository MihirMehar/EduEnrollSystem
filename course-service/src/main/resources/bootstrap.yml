spring:
  application:
    name: course-service  # ✅ ye file ka naam hona chahiye config repo me (student-service.yml)

  cloud:
    config:
      uri: http://localhost:9090  # ✅ Config server ka URI

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true