server:
  port: 9090 # Port for the Config Server

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/MihirMehar/config-server.git # Your Git repository URL


eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/ # Eureka server URL if applicable
